<h1>Listing areas</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Compromiso reduccion</th>
    <th>Compromiso iniciativas</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @areas.each do |area| %>
  <tr>
    <td><%= area.nombre %></td>
    <td><%= area.compromiso_reduccion %></td>
    <td><%= area.compromiso_iniciativas %></td>
    <td><%= link_to 'Show', area %></td>
    <td><%= link_to 'Edit', edit_area_path(area) %></td>
    <td><%= link_to 'Destroy', area, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Area', new_area_path %>
